---
title: 'Ontwikkeling'
description: 'Bekijk wijzigingen lokaal om je documentatie bij te werken'
---

<Info>
  **Voorwaarde**: Installeer Node.js (versie 19 of hoger) voordat je verder gaat.
</Info>

Volg deze stappen om Mintlify op je besturingssysteem te installeren en uit te voeren:

**Stap 1**: Installeer Mintlify:

<CodeGroup>

  ```bash npm
  npm i -g mintlify
  ```

```bash yarn
yarn global add mintlify
```

</CodeGroup>

**Stap 2**: Navigeer naar de documentatie directory (waar het `mint.json` bestand zich bevindt) en voer het volgende commando uit:

```bash
mintlify dev
```

Een lokale preview van je documentatie zal beschikbaar zijn op `http://localhost:3000`.

### Aangepaste Poorten

Standaard gebruikt Mintlify poort 3000. Je kunt de poort waarop Mintlify draait aanpassen met de `--port` flag. Om Mintlify bijvoorbeeld op poort 3333 te draaien, gebruik je dit commando:

```bash
mintlify dev --port 3333
```

Als je probeert Mintlify te draaien op een poort die al in gebruik is, zal het de eerstvolgende beschikbare poort gebruiken:

```md
Poort 3000 is al in gebruik. Probeer 3001 in plaats daarvan.
```

## Mintlify Versies

Let op dat elke CLI-release is gekoppeld aan een specifieke versie van Mintlify. Als je lokale website niet overeenkomt met de productieversie, update dan de CLI:

<CodeGroup>

```bash npm
npm i -g mintlify@latest
```

```bash yarn
yarn global upgrade mintlify
```

</CodeGroup>

## Links Valideren

De CLI kan helpen bij het valideren van referentielinks in je documentatie. Om eventuele verbroken links te identificeren, gebruik je het volgende commando:

```bash
mintlify broken-links
```

## Implementatie

<Tip>
  Onbeperkt aantal editors beschikbaar onder het [Pro
  Plan](https://mintlify.com/pricing) en hoger.
</Tip>

Als de implementatie succesvol is, zou je het volgende moeten zien:

<Frame>
  <img src="/images/checks-passed.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

## Code Opmaak

We raden aan om extensies in je IDE te gebruiken om MDX te herkennen en op te maken. Als je een VSCode-gebruiker bent, overweeg dan de [MDX VSCode extensie](https://marketplace.visualstudio.com/items?itemName=unifiedjs.vscode-mdx) voor syntax highlighting, en [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) voor code opmaak.

## Probleemoplossing

<AccordionGroup>
  <Accordion title='Fout: Kan de "sharp" module niet laden met de darwin-arm64 runtime'>

    Dit kan komen door een verouderde versie van node. Probeer het volgende:
    1. Verwijder de momenteel geïnstalleerde versie van mintlify: `npm remove -g mintlify`
    2. Upgrade naar Node v19 of hoger.
    3. Herinstalleer mintlify: `npm install -g mintlify`
  </Accordion>

  <Accordion title="Probleem: Onbekende fout tegengekomen">
  
    Oplossing: Ga naar de root van je apparaat en verwijder de ~/.mintlify map. Voer daarna opnieuw `mintlify dev` uit.
  </Accordion>
</AccordionGroup>

Benieuwd naar wat er is veranderd in de CLI-versie? [Bekijk de CLI changelog.](https://www.npmjs.com/package/mintlify?activeTab=versions)